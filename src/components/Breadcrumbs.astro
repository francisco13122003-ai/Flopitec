---
const { items=[] } = Astro.props;
---
<nav class="container pt-6 text-sm text-[var(--muted)]" aria-label="Migas de pan"><ol class="flex flex-wrap gap-2"><li><a href="/">Inicio</a></li>{items.map((item)=><li> / <a href={item.href}>{item.label}</a></li>)}</ol></nav>
