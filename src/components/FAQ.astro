---
const { items } = Astro.props;
---
<div class="grid gap-3">{items.map((item)=><details class="card p-5"><summary class="cursor-pointer font-bold">{item.question}</summary><p class="mt-3 text-[var(--muted)]">{item.answer}</p></details>)}</div>
